PORT=8080
# Turso/libSQL. For local dev use file:local.db; for Cloud Run use libsql://...
TURSO_DATABASE_URL=file:local.db
TURSO_AUTH_TOKEN=
# Optional external self-hosted jhaemin/speller-api compatible endpoint. If absent, the app calls nara-speller directly.
SPELLER_API_URL=
# Avoid public abuse.
API_KEY=
